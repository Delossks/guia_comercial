{% extends 'administrador/index.html.twig' %}

{% block title %}{{parent()}} - Buscar Usuario{% endblock %}

{% block body %}

{{parent()}}

<div class="container h-100 " style="margin-top:40px">

    <div class="form-row">
        <div id="campo_email" class="form-group col-md-4">
            <label>Email </label>
            <input type="text" class="form-control" id="email" name="email" placeholder="Email del usuario a buscar" >
        </div>
        <div id="campo_nombre" class="form-group col-md-8">
            <label>Nombre </label>
            <input type="text" class="form-control" id="nombre" name="nombre" placeholder="Nombre del usuario a buscar" >
        </div>
    </div>

    <div class="form-row">
        <div id="campo_apellidos" class="form-group col-md-4">
            <label>Apellidos </label>
            <input type="text" class="form-control" id="apellidos" name="apellidos" placeholder="Apellidos del usuario a buscar" >
        </div>
        <div id="campo_telefono" class="form-group col-md-8">
            <label>Teléfono </label>
            <input type="text" class="form-control" id="telefono" name="telefono" placeholder="Teléfono del usuario a buscar" >
        </div>
    </div>

    <div class="form-row">
        <div id="campo_validez" class="form-group col-md-4">
            <label>Validez </label>
            <input type="text" class="form-control" id="validez" name="validez" placeholder="Validez del usuario a buscar" >
        </div>
    </div>
    
    <button type="button" class="btn btn-primary" onclick="buscar();">Buscar</button>
    
</div>

<script>
    function buscar(){
        email = $("#email").val();
        nombre = $("#nombre").val();
        apellidos = $("#apellidos").val();
        telefono = $("#telefono").val();
        validez = $("#validez").val();
        $("#usuariosEncontrados").empty();
        $.ajax
        ({
            type: "POST",
            cache: true,
            dataType: 'html',
            url: "{{path('buscarUsuario')}}", 
            data: {email: email, nombre: nombre, apellidos: apellidos, telefono: telefono, validez: validez},
            success: function(html)
            {
                // Mostrar el resultado de la búsqueda
                if(html !== ""){
                    $("#usuariosEncontrados").empty();
                    $("#usuariosEncontrados").html(html);
                }
            }
        });
    }
</script>

{# {{dump(usuarios)}} #}


{% if usuarios is not same as("") and usuarios is not null and usuarios is not empty %}
<div class="container h-100 " style="margin-top:40px">
    <table id="tbUsuario" class="table table-hover table-sm">
        <thead style="border-bottom-style:2px solid #24a690;">
            <tr>
                <th>ID</th>
                <th>Email</th>
                {# <th>Rol</th> #}
                <th>Nombre</th>
                <th>Apellidos</th>
                <th>Teléfono</th>
                <th>Fecha Alta</th>
                <th>Validez</th>
                <th style="width:20%"></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                    <div class="scrollit">
                            {% for usuario in usuarios %}
                                <tr id ='{{usuario.id}}'>
                                    <td>{{usuario.id}}</td>
                                    <td>{{usuario.email}}</td>
                                    {# <td>{{usuario.roles}}</td> #}
                                    <td>{{usuario.nombre}}</td>
                                    <td>{{usuario.apellidos}}</td>
                                    <td>{{usuario.telefono}}</td>
                                    <td>{{usuario.fechaAlta | date('d-m-Y  h:m:s')}}</td>
                                    <td>{{usuario.validez}}</td>
                                    <td style="width:19%" align="center">                                   
                                        <button type="button" class="btn btn-link" onclick="location.href='{{ path('validarUsuario', {'id': usuario.id})}}'"><img height="24px" src="{{ asset('images/checks.png') }}" title="Validar"/></button>
                                        <button type="button" class="btn btn-link" onclick="location.href='{{ path('consultarUsuario', {'id': usuario.id})}}'"><img height="24px" src="{{ asset('images/about.png') }}" title="Consultar"/></button>
                                        <button type="button" class="btn btn-link" onclick="location.href='{{ path('modificarUsuario', {'id': usuario.id})}}'"><img height="24px" src="{{ asset('images/contract.png') }}" title="Modificar"/></button> 
                                        <button type="button" class="btn btn-link" onclick="location.href='{{ path('eliminarUsuario', {'id': usuario.id})}}'"><img height="24px" src="{{ asset('images/delete2.png') }}" title="Eliminar"/></button>
                                    </td> 
                                </tr>
                            {% endfor %}
                        </table>
                    </div>
               </td>
            </tr>
        </tbody>
    </table>
</div>
{% else %}
<div class="container h-100 " style="margin-top:40px">
    <p style="text-align:center">No se ha encontrado ningun usuario que satisfaga los criterios seleccionados</p>
</div>
{% endif %}

{% endblock %}