{% extends 'administrador/index.html.twig' %}

{% block title %}{{parent()}} - Registrar Usuario{% endblock %}

{% block body %}

{{parent()}}

{#
    {% for message in app.flashes('exito') %}
        <div class="alert alert-success">
            {{message}}
        </div>
    {% endfor %}
#}

<div class="container h-100 " style="margin-top:60px">
    <div class="row h-100-justify-content-center align-items-center ">
        <div class="col-6 bg-white rounded my-3 p-5">
            <h3>Registro de usuario - Administrador</h3>
            <div style="border: 7px ridge SlateBlue;">
            {{form(formulario)}}
            {{form(formC)}}
            {{form(formE)}}
            {{form(formA)}}
            </div>
            <div style="margin-top:10px">
                <button type="button" class="btn btn-default" onclick="location.href='{{path('buscarUsuario')}}'">Cancelar</button>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block content %}
{% endblock %}