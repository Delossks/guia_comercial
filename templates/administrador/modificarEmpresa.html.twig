{% extends 'administrador/index.html.twig' %}

{% block title %}{{parent()}} - Modificar Empresa{% endblock %}

{% block body %}

{{parent()}}

{#
    {% for message in app.flashes('exito') %}
        <div class="alert alert-success">
            {{message}}
        </div>
    {% endfor %}
#}

<div class="container h-100 " style="margin-top:60px">
    <div class="row h-100-justify-content-center align-items-center ">
        <div class="col-6 bg-white rounded my-3 p-5">
            <h3>Modificar Empresa</h3>
            <div style="border: 7px ridge SlateBlue;">
            {{form(formulario)}}
            </div>
            <div style="margin-top:10px">
                <button type="button" class="btn btn-default" onclick="location.href='{{path('buscarEmpresaAdmin')}}'">Cancelar</button>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block content %}
{% endblock %}
