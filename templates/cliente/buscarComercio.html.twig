{% extends 'cliente/index.html.twig' %}

{% block title %}{{parent()}} - Buscar Comercio{% endblock %}

{% block body %}

{{parent()}}

<div class="container h-100 " style="margin-top:40px">

    <div class="form-row">
        <div id="campo_nombre_comercio" class="form-group col-md-4">
            <label>Nombre </label>
            <input type="text" class="form-control" id="nombre_comercio" name="nombre_comercio" placeholder="Nombre del comercio a buscar" >
        </div>
        <div id="campo_direccion_comercio" class="form-group col-md-8">
            <label>Direccion </label>
            <input type="text" class="form-control" id="direccion_comercio" name="direccion_comercio" placeholder="Dirección del comercio a buscar" >
        </div>
    </div>

    <div class="form-row">
        <div id="campo_codigo_postal" class="form-group col-md-4">
            <label>Código Postal </label>
            <input type="text" class="form-control" id="codigo_postal" name="codigo_postal" placeholder="Código postal del comercio a buscar" >
        </div>
        <div id="campo_telefono_comercio" class="form-group col-md-8">
            <label>Teléfono </label>
            <input type="text" class="form-control" id="telefono_comercio" name="telefono_comercio" placeholder="Teléfono del comercio a buscar" >
        </div>
    </div>
    
    <button type="button" class="btn btn-primary" onclick="buscar();">Buscar</button>
    
</div>

<script>
    function buscar(){
        nombre_comercio = $("#nombre_comercio").val();
        direccion_comercio = $("#direccion_comercio").val();
        codigo_postal = $("#cp_comercio").val();
        telefono_comercio = $("#telefono_comercio").val();
        $("#comerciosEncontrados").empty();
        $.ajax
        ({
            type: "POST",
            cache: true,
            dataType: 'html',
            url: "{{path('buscarComercioEmp')}}", 
            data: {nombre_comercio: nombre_comercio, direccion_comercio: direccion_comercio, codigo_postal: codigo_postal, telefono_comercio: telefono_comercio},
            success: function(html)
            {
                // Mostrar el resultado de la búsqueda
                if(html !== ""){
                    $("#comerciosEncontrados").empty();
                    $("#comerciosEncontrados").html(html);
                }
            }
        });
    }
</script>

{# {{dump(comercios)}} #}

{% if comercios is not same as("") and comercios is not null and comercios is not empty %}
<div class="container h-100 " style="margin-top:40px">
    <table id="tbComercio" class="table table-hover table-sm">
        <thead style="border-bottom-style:2px solid #24a690;">
            <tr>
                <th>Nombre</th>
                <th>Dirección</th>
                <th>Código Postal</th>
                <th>Teléfono</th>
                <th>Web</th>
                <th style="width:20%"></th>
            </tr>
        </thead>
        <tbody>
            <tr>
                    <div class="scrollit">
                            {% for comercio in comercios %}
                                <tr id ='{{comercio.id}}'>
                                    <td>{{comercio.nombreComercio}}</td>
                                    <td>{{comercio.direccionComercio}}</td>
                                    <td>{{comercio.codigoPostal}}</td>
                                    <td>{{comercio.telefonoComercio}}</td>
                                    <td>{{comercio.webComercio}}</td>
                                    <td style="width:19%" align="center">                                   
                                        {# <button type="button" class="btn btn-link" onclick="location.href='{{ path('gestion_consultarDocente', {'correo_docente': docente.correoDocente})}}'"><img height="25px" src="{{ asset('public/images/lupaVerde.jpg') }}" title="Consultar"/></button> #}
                                    </td> 
                                </tr>
                            {% endfor %}
                    </div>
               </td>
            </tr>
        </tbody>
    </table>
</div>
{% else %}
<div class="container h-100 " style="margin-top:40px">
    <p style="text-align:center">No se ha encontrado ningun comercio que satisfaga los criterios seleccionados</p>
</div>
{% endif %}

{% endblock %}