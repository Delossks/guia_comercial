{% extends 'empresario/index.html.twig' %}

{% block title %}{{parent()}} - Modificar Oferta{% endblock %}

{% block body %}

{{parent()}}

<div class="container h-100 " style="margin-top:60px">
    <div class="row h-100-justify-content-center align-items-center ">
        <div class="col-6 bg-white rounded my-3 p-5">
            <h3>Modificar Oferta</h3>
            <div style="border: 7px ridge SlateBlue;">
            {{form(formulario)}}
            </div>
            <div style="margin-top:10px">
                <button type="button" class="btn btn-default" onclick="history.back()">Cancelar</button>
            </div>
{#            
            <div class="container h-100 " style="margin-top:30px; border: 10px ridge SlateBlue;">
        <div class="col-6 bg-white rounded my-3 p-5">

        <div class="form-row" style="margin-top:20px">
            <div id="campo_id_comercio" class="form-group col-md-6">
                {{ form_label(formulario.id_comercio) }}
                {{ form_widget(formulario.id_comercio) }}
            </div>
        </div>

        <div class="form-row">
            <div id="campo_img_oferta" class="form-group col-md-6">
                {{ form_label(formulario.img_oferta) }}
                {{ form_widget(formulario.img_oferta) }} 
                <img src="{{img_oferta}}" /> 
            </div>
        </div>

        <div class="form-row">
            <div id="campo_descripcion" class="form-group col-md-12">
                {{ form_label(formulario.descripcion) }}
                {{ form_widget(formulario.descripcion) }}
            </div>
        </div>

        <div class="form-row">    
            <div id="campo_fecha_inicio" class="form-group col-md-6">
                {{ form_label(formulario.fecha_inicio) }}
                {{ form_widget(formulario.fecha_inicio) }}
            </div>
        </div>

        <div class="form-row">
            <div id="campo_fecha_fin" class="form-group col-md-6">
                {{ form_label(formulario.fecha_fin) }}
                {{ form_widget(formulario.fecha_fin) }}
            </div>
        </div>

        <div class="form-row">
            <div id="boton_volver" class="form-group col-md-7">
                <button type="button" class="btn btn-default" onclick="history.back()">Cancelar</button>
            </div>
        </div>
        #}
        </div>
    </div>
</div>

{% endblock %}

{% block content %}
{% endblock %}